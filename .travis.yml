language: php
dist: trusty
sudo: false

php:
  - 5.6
  - 7.0

mysql:
  database: drupal
  username: root
  encoding: utf8

env:
  global:
    - SIMPLETEST_BASE_URL="http://127.0.0.1:8080"
  matrix:
    - RELEASE=stable COMPOSER_CHANNEL=stable
    - RELEASE=stable COMPOSER_CHANNEL=snapshot

matrix:
  exclude:
    - php: 5.6
      env: RELEASE=dev COMPOSER_CHANNEL=stable
    - php: 5.6
      env: RELEASE=stable COMPOSER_CHANNEL=snapshot

before_install:
  - if [[ $TRAVIS_PHP_VERSION = 5.6 ]]; then export COMPOSER_MEMORY_LIMIT=-1; fi;
  - echo 'sendmail_path = /bin/true' >> ~/.phpenv/versions/$(phpenv version-name)/etc/conf.d/travis.ini
  - phpenv config-rm xdebug.ini
  - composer --verbose self-update --$COMPOSER_CHANNEL
  - composer --version

install:
  - composer --verbose validate
  - composer --verbose install
  - composer global require consolidation/cgr
  - export PATH="$HOME/.composer/vendor/bin:$PATH"
  - cgr drush/drush:8.1.15

script:
  - cd $TRAVIS_BUILD_DIR/web
  - drush site-install config_installer config_installer_sync_configure_form.sync_directory=../config/sync --verbose --yes --db-url=mysql://root:@localhost/drupal
  - drush runserver $SIMPLETEST_BASE_URL &
  - until curl -s $SIMPLETEST_BASE_URL; do true; done > /dev/null
  # Skip core/tests/Drupal/Tests/ComposerIntegrationTest.php because web/ has no composer.json
  # Ignore PageCache group temporarily, @see https://www.drupal.org/node/2770673
  # Ignore Setup group temporarily, @see https://www.drupal.org/node/2962157
  # Ignore webform tests temporarily. TODO: try to newer version of webform and remove again
  - ./../vendor/bin/phpunit -c core --testsuite unit --exclude-group Composer,DependencyInjection,PageCache,Setup,webform
  - drush
  - if [[ $RELEASE = stable ]]; then ./../vendor/bin/drupal; fi;
